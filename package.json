{
  "main": "build/index.js",
  "scripts": {
    "build": "[ -d build ] && rm -rf build; cp -r src build",
    "start": "npm run build && node .",
    "cov": "npm run build && istanbul cover node_modules/mocha/bin/_mocha -- -u exports -R spec test/**.spec.js",
    "test": "npm run build && mocha test/**.spec.js",
    "prepublish": "npm run build"
  },
  "license": "MIT",
  "dependencies": {
    "http-auth-payload": "^0.3.1",
    "lodash": "^4.16.3",
    "toml": "^2.3.0",
    "ws": "^1.1.1"
  },
  "devDependencies": {
    "istanbul": "^0.4.5",
    "mocha": "^3.1.0"
  },
  "name": "service-proxy",
  "version": "0.1.0",
  "description": "Network service proxy",
  "bin": {
    "service-proxy": "cli.js"
  },
  "directories": {
    "example": "examples",
    "test": "test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rumkin/service-proxy.git"
  },
  "author": "dev@rumk.in",
  "bugs": {
    "url": "https://github.com/rumkin/service-proxy/issues"
  },
  "homepage": "https://github.com/rumkin/service-proxy#readme"
}
